import 'package:flutter/material.dart';

class StickerPacksListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("WA Stickers"),
      ),
      body: Container(),
    );
  }
}
